apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sm-notification-center
  namespace: sm-platform
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "1m"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - sm-notification-center.sm-platform.example.com
      secretName: sm-notification-center-tls
  rules:
    - host: sm-notification-center.sm-platform.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sm-notification-center
                port:
                  number: 8300
