apiVersion: v1
kind: Service
metadata:
  name: sm-notification-center
  namespace: sm-platform
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8300
      targetPort: http
  selector:
    app: sm-notification-center
